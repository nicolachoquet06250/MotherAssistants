@html.extend('layout', function(model) {
	@html.block('menu', function(model) {
		@html.include('includes/menus/top_menu', model)
	})
	@html.block('content', function(model) {
		<h1>@model.title</h1>
		<p>Welcome to @model.title</p>
	})
})
